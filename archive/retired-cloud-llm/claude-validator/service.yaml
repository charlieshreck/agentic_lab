apiVersion: v1
kind: Service
metadata:
  name: claude-validator
  namespace: ai-platform
  labels:
    app: claude-validator
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: http
    nodePort: 30201
    protocol: TCP
    name: http
  selector:
    app: claude-validator
