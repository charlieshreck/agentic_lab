apiVersion: v1
kind: Service
metadata:
  name: claude-validator
  namespace: ai-platform
  labels:
    app: claude-validator
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: claude-validator
