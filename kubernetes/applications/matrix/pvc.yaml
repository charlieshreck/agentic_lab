apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: conduit-data
  namespace: ai-platform
  labels:
    app: conduit
    component: matrix-server
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
