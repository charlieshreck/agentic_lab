---
# InfisicalSecret that syncs Gemini OAuth credentials from Infisical
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: gemini-credentials-sync
  namespace: ai-platform
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /agentic-platform/gemini
  managedSecretReference:
    secretName: gemini-credentials
    secretNamespace: ai-platform
    secretType: Opaque
    creationPolicy: Owner
