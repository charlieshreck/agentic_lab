---
# Gemini API credentials for LiteLLM
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: gemini-credentials
  namespace: ai-platform
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /agentic-platform/Gemini
  managedSecretReference:
    secretName: gemini-credentials
    secretNamespace: ai-platform
    secretType: Opaque
    creationPolicy: Owner
---
# OpenRouter API credentials (fallback for rate limits)
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: openrouter-credentials
  namespace: ai-platform
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /agentic-platform/openrouter
  managedSecretReference:
    secretName: openrouter-credentials
    secretNamespace: ai-platform
    secretType: Opaque
    creationPolicy: Owner
---
# Anthropic API credentials for Claude models (tier3)
# NOTE: Add ANTHROPIC_API_KEY to Infisical at /agentic-platform/anthropic
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: anthropic-credentials
  namespace: ai-platform
spec:
  hostAPI: https://app.infisical.com/api
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical-operator-system
      secretsScope:
        projectSlug: prod-homelab-y-nij
        envSlug: prod
        secretsPath: /agentic-platform/anthropic
  managedSecretReference:
    secretName: anthropic-credentials
    secretNamespace: ai-platform
    secretType: Opaque
    creationPolicy: Owner
