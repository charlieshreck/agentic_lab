apiVersion: v1
kind: Service
metadata:
  name: redis-nodeport
  namespace: ai-platform
  labels:
    app: redis
    component: cache
spec:
  type: NodePort
  ports:
  - port: 6379
    targetPort: 6379
    nodePort: 30637
    protocol: TCP
    name: redis
  selector:
    app: redis
