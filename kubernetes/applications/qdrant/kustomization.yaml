apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-platform

resources:
  - namespace.yaml
  - statefulset.yaml
  - service.yaml
  # Collection initialization is handled by sidecar in statefulset.yaml
  # No separate Jobs needed - fully IaC compliant
