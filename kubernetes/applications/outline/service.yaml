apiVersion: v1
kind: Service
metadata:
  name: outline
  namespace: ai-platform
  labels:
    app.kubernetes.io/name: outline
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: outline
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 31113
      protocol: TCP
