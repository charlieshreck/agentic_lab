apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-platform

resources:
  - langgraph.yaml
  - seed-runbooks.yaml  # ConfigMap with runbook definitions (reference data)
  - ingress.yaml
  - infisical-secret.yaml
  # Runbooks are seeded in Qdrant via knowledge-mcp or manual ingestion
  # The ConfigMap provides the canonical runbook definitions
