apiVersion: v1
kind: Service
metadata:
  name: kernow-hub-nodeport
  namespace: ai-platform
spec:
  type: NodePort
  selector:
    app: kernow-hub
  ports:
  - port: 3456
    targetPort: 3456
    nodePort: 30456
    name: http
