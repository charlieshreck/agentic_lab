apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fumadocs
  namespace: ai-platform
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - fumadocs.kernow.io
      secretName: kernow-io-tls
  rules:
    - host: fumadocs.kernow.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fumadocs
                port:
                  number: 3000
