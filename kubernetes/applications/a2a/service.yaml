apiVersion: v1
kind: Service
metadata:
  name: a2a-orchestrator
  namespace: ai-platform
  labels:
    app: a2a-orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: a2a-orchestrator
