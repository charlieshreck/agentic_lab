apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-platform

resources:
  - infisical-secrets.yaml
  - configmap.yaml
  - context-configmap.yaml
  - deployment.yaml
  - token-expiry-monitor.yaml
  # Note: mcp-servers-config ConfigMap is in platform/mcp-config/
  # and referenced via envFrom in deployment.yaml
