apiVersion: v1
kind: Service
metadata:
  name: postgresql-nodeport
  namespace: ai-platform
  labels:
    app: postgresql
    component: database
spec:
  type: NodePort
  ports:
  - port: 5432
    targetPort: 5432
    nodePort: 30543
    protocol: TCP
    name: postgres
  selector:
    app: postgresql
