apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-platform

resources:
  - configmap.yaml
  - infisical-secrets.yaml
  # NOTE: All individual MCP servers have been consolidated into domain MCPs
  # deployed from mcp-servers repo via the domain-mcps ArgoCD app.
  # Legacy MCPs (arr-suite, plex, home-assistant, tasmota, unifi, adguard,
  # homepage, proxmox, truenas, cloudflare, opnsense, infisical, vikunja,
  # outline) have been removed. ArgoCD prune=true will clean up live resources.
  # Web search (SearXNG backend - still needed by external-mcp)
  - searxng.yaml
  # Knowledge reconciliation
  - knowledge-reconcile-cronjob.yaml
