apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NodePort services for agentic cluster
# No Traefik needed - Caddy on OPNsense handles TLS termination

resources:
  - nodeport-services.yaml
