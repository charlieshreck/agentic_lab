apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Traefik ingress controller for agentic cluster
# Coexists with NodePort+Caddy pattern during migration

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingressclass.yaml
  - nodeport-services.yaml  # Keep existing NodePorts working
