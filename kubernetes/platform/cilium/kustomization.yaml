apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: quick-install.yaml is managed by cilium-app.yaml (Helm chart)
# This kustomization only manages LB IP Pool and L2 policy

resources:
  - lb-ippool.yaml
  - l2-policy.yaml
