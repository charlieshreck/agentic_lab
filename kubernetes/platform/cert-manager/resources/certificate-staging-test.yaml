apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: agentic-staging-test
  namespace: cert-manager
spec:
  secretName: agentic-staging-test-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - traefik-test.kernow.io
